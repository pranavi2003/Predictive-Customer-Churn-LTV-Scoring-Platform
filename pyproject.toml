[project]
name = "churn-platform"
version = "1.0.0"
description = "Real-time customer churn prediction and LTV scoring platform"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
